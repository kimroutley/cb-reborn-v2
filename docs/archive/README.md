# Archive

This directory stores historical snapshots, superseded plans, and one-off operational artifacts.

## Archival policy

- Keep release evidence immutable in `docs/releases/<date>/`.
- Move superseded planning docs here instead of deleting.
- Prefer adding a short provenance header with original location/date when archiving.

## Current migration state

Root-level historical markdown files are pending relocation in the next migration pass.

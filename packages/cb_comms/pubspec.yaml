name: cb_comms
description: "Club Blackout communication layer - Firebase integration and cloud sync"
publish_to: 'none'
version: 1.0.0
environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.10.0"
dependencies:
  flutter:
    sdk: flutter
  cb_models:
    path: ../cb_models
  firebase_core: ^4.4.0
  firebase_auth: ^6.1.4
  cloud_firestore: ^6.1.2

  web_socket_channel: ^3.0.3
  json_annotation: ^4.9.0
  shared_preferences: ^2.5.4
  connectivity_plus: ^7.0.0
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
